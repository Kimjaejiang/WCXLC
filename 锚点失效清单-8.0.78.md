# WXPRO 微信 8.0.78 锚点失效清单

> 数据来源：真机 `P7LR45CINNH6AU9T`（一加 PHP110 / Android 16），微信 8.0.78 (3180)，
> 模块 `260921120000`，日志 `wcx-2026-09-21.log` 11:28 主进程加载。
>
> **本文只陈述实测所得。**「影响」列凡是我推断的，都标 `推断`；有代码佐证的标具体行号。

## 总览

| | 功能数 | 锚点数 |
|---|---|---|
| 已启用但锚点落空（当前就在空转） | 3 | 5 |
| 未启用但锚点已坏（开了会失效） | 15 | 43 |
| **合计** | **18** | **48** |

微信官方健康检查报的是「312 个功能全部正常」。

**注意**：本清单是**第二版**。第一版只报了 14 个锚点 —— 因为
`DexMethodDelegate.isPlaceholder` 判据恒为 `false`，**所有 method 类锚点的落空都被漏报**。
判据修复后（见下文「附带修复」），数字从 14 涨到 48。
**第一版结论作废，以本文为准。**

---

## 一、已启用 · 当前就在空转（3 个功能 / 5 个锚点）

```
会话列表 View 绑定监听服务  WeConversationListViewApi:methodLegacyGetView
朋友圈菜单增强扩展          WeMomentsContextMenuApi:methodImproveMultiPhotoOnItemSelected
                          WeMomentsContextMenuApi:methodImproveOnItemSelectedRegister2
                          WeMomentsContextMenuApi:methodImproveOnItemSelectedRegister3
消息发送服务                WeMessageApi:ctorNetSceneUploadMsgImg
```

### 1a. 消息发送服务 — `ctorNetSceneUploadMsgImg`

- **位置**：`WeMessageApi.kt:1118`
- **证据**：`:514` `setPlaceholderDescriptor()` —— 走新版本分支时**主动**置空；`:520` 老版本分支才 find 它
- **判定**：**设计内的版本分支，不是破损。**
- **影响**：**无。** 8.0.78 走 `>= MM_8_0_67` 新路径。
- **结论**：假阳性（我的检查器无法区分「设计内缺席」与「真破损」，见文末）。

### 1b. 会话列表 View 绑定监听服务 — `methodLegacyGetView`

- **证据**：`WeConversationListViewApi.kt` 中有 `isPlaceholder` 使用；锚点名带 `Legacy` 前缀
- **判定**：**推断**为设计内降级（老接口在新版被替换），有 `Legacy` 命名与守卫佐证
- **影响**：**推断无。** 模块应已走新接口路径。

### 1c. 朋友圈菜单增强扩展 — 3 个 `methodImproveOnItemSelected*`

- **证据**：`WeMomentsContextMenuApi.kt` 中有 `isPlaceholder` 使用
- **判定**：**推断**为设计内降级
- **影响**：**推断无。**

> 1b / 1c 我**没有逐行核实**调用点是否都有守卫。这两条建议你按需确认，不要当结论用。

---

## 二、未启用 · 启用后会失效（15 个功能 / 43 个锚点）

### 2.1 音视频通话使用画中画 — `PipVoip`（8 个）★ 已修

```
classMultiTalkViewModel      fieldMultiTalkViewModel
fieldMultiTalkMicState       fieldMultiTalkCameraState
methodMultiTalkMinimize      methodMultiTalkExit
methodMultiTalkMic           methodMultiTalkCamera
```

- **位置**：`PipVoip.kt:295-385`
- **证据**：缓存 `dex_cache/音视频通话使用画中画.json` 中这 8 个键的值全是
  `Lcom/tencent/mm/ui/LauncherUI;->getInstance()...` / `->INSTANCE:...`
- **判定**：**真实破损。** 微信 8.0.78 里 `MultiTalkUIViewModel` 已不存在。
- **影响**：**会抛异常。** `GroupSession` 的每个方法都踩坏锚点：
  - `:119,125` 读 `fieldMultiTalkMicState.field` / `fieldMultiTalkCameraState.field`
  - `:130` `methodMultiTalkExit.method`、`:134` `methodMultiTalkMic.method`、`:138` `methodMultiTalkCamera.method`
  - `:141` `fieldMultiTalkViewModel.field`
  - `:508` `methodMultiTalkMinimize.hookBefore`（装 hook 时即抛）
  - 委托的 `.field` / `.method` 对占位符直接 `error()`
    （`DexDelegates.kt:172`、`:291`）
  - 触发：**群视频通话 + 画中画**。单人通话走 `SingleSession`(`:95-106`) 用
    `fieldVoipAudioManager` 等**命中**的锚点，不受影响。
- **已修**：见下节「已实施的修复」。

### 2.2 分裂群组通话 — `SplitGroupCall`（14 个）

```
classSubCoreMultiTalk       methodExitMultiTalk        ctorInviteTask
classILinkService           methodGetMultiTalkManager  ctorHangupTask
classILinkMember            methodSetStatus            fieldILinkInstance
classInviteTask             methodSetMtSdkMode         fieldRoomId
                            methodSetName              methodPostTask
```

- **位置**：`SplitGroupCall.kt:87-135`
- **证据**：缓存 `dex_cache/分裂群组通话.json`，值全为 `LauncherUI` 占位符
- **判定**：**真实破损。** legacy MultiTalk / ILink 架构在 8.0.78 已不存在。
- **影响**：**VOIP 模式不可用，但已优雅降级。**
  - `:394` VOIP 模式从可选项里自动剔除
  - `:499` `check(...)` 兜底
  - 开发者注释 `:189-194` 明说 8.0.78 起日志串消失 → **已知问题**
- **对讲机模式仍然可用**（`methodEnterTalkRoom` 等 4 个锚点**命中**，
  缓存值为真实的 `Lqo4/f0;->h(...)`）
- **结论**：**无需新动作。**

### 2.3 其余 13 个功能 / 21 个锚点

| 功能 | 坏锚点数 | 锚点 |
|---|---|---|
| 隐藏对话列表分割线 | 4 | `methodConversationWithCacheAdapterGetView`, `methodDispatchChildAttached`, `methodRecyclerOnBind`, `methodSupportRecyclerOnBind` |
| 隐藏联系人 | 3 | `methodExitMultiTalk`, `methodMultiTalkOnInvite`, `methodVoipLegacyInsertMsg` |
| 发现页入口隐藏 | 2 | `methodDiscoverUiOnCreate`, `methodDiscoverUiOnResume` |
| 发现页朋友圈入口隐藏 | 2 | `methodDiscoverUiOnCreate`, `methodDiscoverUiOnResume` |
| 解除消息多选数量限制 | 2 | `methodGetSelectedMessageCount`, `methodToggleMessageSelection` |
| 存储空间缓存隐藏 | 1 | `methodCleanCacheListInstaller` |
| 存储空间聊天记录隐藏 | 1 | `methodCleanRecordListInstaller` |
| 状态页隐藏 | 1 | `methodStatusListInstaller` |
| 离开对话/离开微信隐藏 | 1 | `methodLauncherFocusChanged` |
| 隐藏最近转发 | 1 | `methodTransmitListInstaller` |
| 屏蔽铃声 | 1 | `methodPlaySoundByName` |
| 左划对话菜单 | 1 | `classConversationAdapter` |
| 自动同意好友申请 | 1 | `methodVerifyOkClick` |

**这 13 个我尚未逐个核实调用点是否有 `isPlaceholder` 守卫。**
其中「左划对话菜单」已确认是设计内降级（`:167-168` 注释 + `:197` 守卫 + 兄弟锚点
`classMvvmConversationAdapter` 命中）。

**这批里有多个 `LauncherUI;->getInstance()` 占位符**（抽查确认：
`HideConversationListDividers`、`RemoveMessageSelectionLimit`、`BlockVoipRingtone`）
—— 即这些功能的 method 锚点在 8.0.78 确实全落空。

**建议**：按「有没有配套守卫」筛一遍。**没守卫的才会在启用时崩或静默失效。**

---

## 已实施的修复

### 修复 1（本次新增）：`PipVoip` 群通话画中画崩溃

`PipVoip.kt:114-166`，`GroupSession` 加统一守卫：

```kotlin
private val multiTalkHooksAvailable: Boolean by lazy {
    !fieldMultiTalkViewModel.isPlaceholder &&
    !fieldMultiTalkMicState.isPlaceholder &&
    !fieldMultiTalkCameraState.isPlaceholder &&
    !methodMultiTalkExit.isPlaceholder &&
    !methodMultiTalkMic.isPlaceholder &&
    !methodMultiTalkCamera.isPlaceholder
}
```

各属性/方法前置 `if (!multiTalkHooksAvailable) return <默认值>`；
`hangUp()` 退回 `groupActivity.finish()`（保证用户能退出，不困在界面）；
`:508` 的 `methodMultiTalkMinimize.hookBefore` 加 `isPlaceholder` 跳过。

默认值 `micMuted=false` / `videoEnabled=true` 对齐 `Session` 基类 `:48-49`。

### 修复 2（附带发现，影响面更大）：`DexMethodDelegate` 判据恒假

**三个同源 bug，都是拿「方法名」比「整条描述符」：**

| 位置 | 原判据 | 后果 |
|---|---|---|
| `:309` `isPlaceholder` | `descriptor!!.name == "Lcom/...->getInstance()..."` | **恒 false** → method 锚点落空永不报出 |
| `:291` `method` 守卫 | `descriptor!!.name == "Lcom/...->()Lcom/..."` | **恒 false** → 占位符不走 `error()`，报误导性 `Method not found` |
| `:433` constructor `isPlaceholder` | `endsWith(...)` | 正确但脆弱 |

根因：`DexMethodDescriptor.name` 是 `desc.substring(a+2, b)`
（`DexMethodDescriptor.kt:21`），**只取方法名** `getInstance`。

**修法**：三处统一为 `descriptor?.descriptor == PLACEHOLDER_DESCRIPTOR`，
哨兵串提为 `DexMethodDelegate.PLACEHOLDER_DESCRIPTOR` 常量，
`setPlaceholderDescriptor` 复用同一常量（消除三处重复字面量）。

**这个修复让检出数从 14 涨到 48。**

**连带效应**：`SplitGroupCall.kt:195` 的
`walkieTalkieAvailable = !methodEnterTalkRoom.isPlaceholder` **原本恒为 true** ——
降级逻辑是死的。现在才真正生效（当前对讲机锚点确实命中，所以结论不变，
但**将来若落空能正确检出**）。

---

## 结论：需要动作的只有 1 项，已做

| 功能 | 性质 | 状态 |
|---|---|---|
| 音视频通话使用画中画 | 真实破损，无守卫 → 群通话 PiP 抛异常 | **已修，待真机验证** |
| 分裂群组通话 | 真实破损，已有降级 | 无需动作 |
| 其余 16 个 | 多数疑似设计内降级，**未逐个核实** | 建议按守卫筛一遍 |

---

## 附：我的检查器已知的假阳性

「消息发送服务」这类**不是问题却仍被报出**。原因：采集只看 `isPlaceholder`，
**无法区分**：

1. 锚点本该不存在（版本分支、`allowFailure` 设计）→ 无害
2. 锚点本该存在但没找到（真破损）→ 有害

代码里区分二者的唯一线索是**调用点有没有 `isPlaceholder` 守卫**。
`SwipeConversationOperations:197` 有 → 无害；`PipVoip` 原无 → 有害。

**这个信息在 FeatureHealth 层拿不到**（它只知委托状态，不知调用点）。
要消除假阳性，需功能侧自报「我容忍哪些锚点缺席」。**是设计缺口。**
